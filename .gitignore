# Env files (per-machine secrets)
**/.env.local
**/.env.*.local

# Gradle build artifacts and caches (per-machine)
**/.gradle/
**/build/
**/bin/

# IDE settings (per-user)
.vscode/
.idea/
*.iml

# Runtime SQLite databases (mounted by docker)
data/*.db

# OS junk
.DS_Store
Thumbs.db
